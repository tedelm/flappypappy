// Copy to config.js. Do not commit config.js — it contains your GET API key.
// RUN_HMAC_SECRET belongs only on the server (/etc/flappy/scoreapi.env).
window.FLAPPY_SCORE_API_URL = "https://flappy-pappy.se";
window.FLAPPY_SCORE_API_KEY = "YOUR_API_KEY";
